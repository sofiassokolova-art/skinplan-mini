# 🚀 Деплой на Vercel - Готово!

## ⚠️ Важно: Настроена гибридная архитектура

У вас есть:
- **Фронтенд на Vite** (`src/pages/`) - уже работает
- **API на Next.js** (`app/api/`) - новый

Vercel будет использовать Next.js только для API роутов, а старый фронтенд продолжит работать.

## 📋 Перед деплоем

### 1. Проверьте переменные окружения на Vercel

Зайдите в **Settings → Environment Variables** и добавьте:

- **DATABASE_URL** - строка подключения к Neon
- **TELEGRAM_BOT_TOKEN** - `8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw`
- **JWT_SECRET** - сгенерированный ключ

### 2. Закоммитьте изменения

```bash
git add .
git commit -m "Add Next.js API integration"
git push origin main
```

## 🚀 Деплой

### Вариант 1: Через Vercel Dashboard (автоматически)

Если проект уже подключен к Vercel:
1. Просто сделайте `git push` - Vercel задеплоит автоматически

### Вариант 2: Через Vercel CLI

```bash
# Логин (если еще не залогинены)
npx vercel login

# Деплой
npx vercel

# Production деплой
npx vercel --prod
```

## ✅ После деплоя

1. Проверьте, что API работает:
   ```
   https://your-app.vercel.app/api/questionnaire/active
   ```

2. Проверьте старые страницы:
   ```
   https://your-app.vercel.app/
   https://your-app.vercel.app/quiz
   ```

3. Откройте мини-апп в Telegram и протестируйте!

## 🔧 Если что-то не работает

- Проверьте логи на Vercel Dashboard
- Убедитесь, что все переменные окружения добавлены
- Проверьте, что база данных заполнена

