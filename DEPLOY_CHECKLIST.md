# ✅ Чек-лист перед деплоем на Vercel

## 📋 Что проверить

### 1. Конфигурация
- [x] `vercel.json` настроен
- [x] `next.config.mjs` создан (ES модуль)
- [x] `package.json` содержит правильные скрипты

### 2. Переменные окружения на Vercel

Нужно добавить в **Settings → Environment Variables** на Vercel:

1. **DATABASE_URL**
   - Значение: строка подключения к Neon базе
   - Окружения: Production, Preview, Development

2. **TELEGRAM_BOT_TOKEN**
   - Значение: `8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw`
   - Окружения: Production, Preview, Development

3. **JWT_SECRET**
   - Значение: сгенерированный секретный ключ
   - Окружения: Production, Preview, Development
   - Можно сгенерировать: `openssl rand -base64 32`

### 3. База данных
- [x] База Neon создана (`neon-red-yacht`)
- [x] Анкета заполнена (`npm run seed:questionnaire-full`)
- [x] Продукты и правила созданы

## 🚀 Команды для деплоя

### Вариант 1: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Или локально
npm install -D vercel

# Логин в Vercel
npx vercel login

# Деплой
npx vercel

# Деплой в production
npx vercel --prod
```

### Вариант 2: Через Git (автоматический)

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push origin main
   ```

2. Vercel автоматически задеплоит при push в main

## ⚠️ Важные моменты

1. **Переменные окружения** должны быть добавлены на Vercel ДО деплоя
2. **База данных** должна быть заполнена (можно сделать через Vercel CLI или локально)
3. **Build команда** должна работать: `npm run build`

## 🔍 После деплоя

1. Проверьте, что сайт открывается
2. Проверьте API: `https://your-app.vercel.app/api/questionnaire/active`
3. Проверьте Quiz: `https://your-app.vercel.app/quiz`
4. Откройте мини-апп в Telegram и протестируйте

