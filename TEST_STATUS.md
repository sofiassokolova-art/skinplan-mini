# ✅ Статус тестирования

## 📋 Инструкции для тестирования

### Вариант 1: Локальное тестирование (быстро)

1. **Запустите сервер:**
   ```bash
   npm run dev
   ```

2. **Проверьте API:**
   - Откройте в браузере: http://localhost:3000/api/questionnaire/active
   - Должен вернуть JSON с анкетой

3. **Проверьте Quiz:**
   - Откройте: http://localhost:3000/quiz
   - Должна открыться страница анкеты

### Вариант 2: Автоматический тест

```bash
# Запустите сервер в одном терминале
npm run dev

# В другом терминале запустите тест
node test-api.js
```

### Вариант 3: Тестирование в Telegram

1. Задеплойте на Vercel
2. Откройте мини-апп в Telegram
3. Пройдите анкету
4. Проверьте консоль (F12) на ошибки

## 🔍 Что проверять

### ✅ Работает правильно:
- API возвращает анкету
- Quiz страница открывается
- Можно отвечать на вопросы
- Ответы сохраняются в localStorage
- При завершении переходит на /plan

### ⚠️ Ожидаемое поведение:
- Если не в Telegram - токен не будет (это нормально)
- Ответы сохраняются только в localStorage без токена
- С токеном - дополнительно сохраняются через API

### ❌ Проблемы:
- API не отвечает → проверьте сервер и .env
- Ошибки в консоли → смотрите детали ошибок
- Анкета не загружается → проверьте seed-questionnaire-full

## 📝 Чек-лист

- [ ] Сервер запущен (`npm run dev`)
- [ ] API отвечает (`/api/questionnaire/active`)
- [ ] Quiz страница открывается (`/quiz`)
- [ ] Можно отвечать на вопросы
- [ ] Ответы сохраняются в localStorage
- [ ] После завершения переход на `/plan`
- [ ] Нет ошибок в консоли браузера

## 🐛 Отладка

Если что-то не работает, проверьте:

1. **Сервер запущен?**
   ```bash
   lsof -ti:3000
   ```

2. **База данных подключена?**
   ```bash
   npm run db:studio
   ```

3. **Анкета заполнена?**
   ```bash
   npm run seed:questionnaire-full
   ```

4. **Переменные окружения?**
   - DATABASE_URL
   - TELEGRAM_BOT_TOKEN (опционально)
   - JWT_SECRET

