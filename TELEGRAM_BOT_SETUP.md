# Настройка Telegram бота

## Что было добавлено:

1. **Webhook endpoint** - `/api/telegram/webhook`
   - Обрабатывает команду `/start` с приветствием
   - Отправляет кнопку с ссылкой на мини-апп
   - Поддерживает команду `/help`

2. **Скрипт установки webhook** - `scripts/setup-telegram-webhook.ts`

## Настройка:

### 1. Добавить переменные окружения в Vercel:

- `TELEGRAM_BOT_TOKEN` - уже добавлен ✅
- `TELEGRAM_SECRET_TOKEN` (опционально, но рекомендуется для безопасности)
- `NEXT_PUBLIC_MINI_APP_URL` (опционально, по умолчанию используется `https://skinplan-mini.vercel.app`)

### 2. Установить webhook:

**Вариант 1: Через скрипт (после деплоя)**
```bash
npm run telegram:webhook
```

**Вариант 2: Вручную через браузер/curl:**
```bash
# Замените YOUR_TOKEN на ваш TELEGRAM_BOT_TOKEN
# Замените YOUR_SECRET на TELEGRAM_SECRET_TOKEN (если настроен)
curl "https://api.telegram.org/botYOUR_TOKEN/setWebhook?url=https://skinplan-mini.vercel.app/api/telegram/webhook&secret_token=YOUR_SECRET"
```

**Вариант 3: Через Vercel URL (после деплоя):**
1. Откройте Vercel Dashboard → Ваш проект
2. Скопируйте URL деплоя (например, `skinplan-mini-xyz.vercel.app`)
3. Выполните:
```bash
curl "https://api.telegram.org/botYOUR_TOKEN/setWebhook?url=https://skinplan-mini-xyz.vercel.app/api/telegram/webhook"
```

### 3. Проверить работу:

1. Откройте ваш бот в Telegram
2. Отправьте команду `/start`
3. Должно появиться приветственное сообщение с кнопкой "🚀 Открыть SkinIQ"
4. При нажатии на кнопку откроется мини-апп

## Генерация TELEGRAM_SECRET_TOKEN (опционально):

Для безопасности рекомендуется добавить секретный токен:

```bash
node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
```

Скопируйте результат и добавьте в Vercel Environment Variables как `TELEGRAM_SECRET_TOKEN`.

## Структура сообщения /start:

- 👋 Приветствие с именем пользователя
- Описание возможностей SkinIQ
- Кнопка для открытия мини-аппа

## Дополнительные команды:

- `/help` - показать справку

Можно добавить больше команд, редактируя `app/api/telegram/webhook/route.ts`.

