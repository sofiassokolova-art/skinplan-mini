# 🧪 Начало тестирования

## ⚠️ Важно: Исправлена ошибка конфигурации

Обнаружена проблема с `next.config.js` - файл использует CommonJS, а проект настроен на ES модули.

**Решение:** Создан `next.config.mjs` (ES модуль формат).

## 🚀 Инструкции для тестирования

### 1. Запустите сервер вручную

```bash
npm run dev
```

Сервер должен запуститься на http://localhost:3000

### 2. Проверьте API

Откройте в браузере:
```
http://localhost:3000/api/questionnaire/active
```

Должен вернуться JSON с анкетой.

### 3. Проверьте Quiz страницу

Откройте:
```
http://localhost:3000/quiz
```

Должна открыться страница анкеты.

### 4. Проверьте консоль браузера

Нажмите F12 и откройте:
- **Console** - проверьте на ошибки
- **Network** - проверьте запросы к API

## ✅ Что должно работать

1. ✅ API `/api/questionnaire/active` возвращает анкету
2. ✅ Страница `/quiz` открывается
3. ✅ Можно отвечать на вопросы
4. ✅ Ответы сохраняются в localStorage
5. ✅ После завершения переход на `/plan`

## 🔧 Если сервер не запускается

1. **Проверьте конфигурацию:**
   ```bash
   ls -la next.config.*
   ```
   Должен быть `next.config.mjs`

2. **Проверьте .env:**
   ```bash
   grep DATABASE_URL .env
   ```

3. **Запустите вручную и смотрите ошибки:**
   ```bash
   npm run dev
   ```

## 📱 Тестирование в Telegram

После успешного локального тестирования:

1. Задеплойте на Vercel
2. Откройте мини-апп в Telegram
3. Пройдите анкету
4. Проверьте авторизацию и сохранение ответов

