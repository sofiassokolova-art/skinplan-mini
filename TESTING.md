# 🧪 План тестирования

## ✅ Чек-лист для проверки

### 1. API роуты

#### `/api/questionnaire/active`
- [ ] Запрос возвращает анкету с группами и вопросами
- [ ] Структура данных правильная
- [ ] Все 21 вопрос присутствуют

#### `/api/auth/telegram`
- [ ] Принимает `initData` от Telegram
- [ ] Валидирует подпись
- [ ] Создает/обновляет пользователя в БД
- [ ] Возвращает JWT токен

#### `/api/questionnaire/answers`
- [ ] Принимает ответы пользователя
- [ ] Сохраняет в БД
- [ ] Рассчитывает профиль кожи
- [ ] Возвращает профиль

### 2. Интеграция в Quiz.tsx

#### Авторизация
- [ ] При открытии в Telegram автоматически авторизуется
- [ ] Токен сохраняется в localStorage
- [ ] Если не в Telegram - продолжает работать без токена

#### Сохранение ответов
- [ ] При завершении анкеты сохраняет через API
- [ ] Если API недоступен - продолжает работать через localStorage
- [ ] Не ломает существующую функциональность

### 3. Работа фронтенда

- [ ] Страница `/quiz` открывается
- [ ] Анкета отображается
- [ ] Можно проходить анкету
- [ ] Ответы сохраняются в localStorage
- [ ] После завершения переходит на `/plan`

## 🧪 Команды для тестирования

### Проверка API локально

```bash
# Запустить сервер
npm run dev

# Проверить анкету
curl http://localhost:3000/api/questionnaire/active

# Проверить авторизацию (нужен initData от Telegram)
curl -X POST http://localhost:3000/api/auth/telegram \
  -H "Content-Type: application/json" \
  -d '{"initData": "..."}'
```

### Проверка в браузере

1. Откройте http://localhost:3000/quiz
2. Пройдите анкету
3. Проверьте консоль браузера на ошибки
4. Проверьте Network tab - должны быть запросы к API

### Проверка в Telegram

1. Откройте мини-апп в Telegram
2. Пройдите анкету
3. Проверьте авторизацию в консоли
4. Проверьте сохранение ответов

