# 📋 План миграции на новую архитектуру

## Текущая ситуация

- ✅ Мини-апп работает на Vite + React Router
- ✅ Задеплоен на Vercel: `skinplan-mini-5j5d1ce9x-sofias-projects-abc2e7ea.vercel.app`
- ✅ Все страницы работают (Home, Quiz, Plan, Cart, Photo, Insights)
- ✅ Данные хранятся в localStorage
- ✅ База данных Neon уже подключена (`neon-red-yacht`)

## Новая архитектура

- ✅ База данных настроена (Prisma)
- ✅ API роуты созданы (Next.js App Router)
- ✅ Полная анкета перенесена в БД
- ✅ Продукты и правила созданы

## 🎯 Стратегия миграции

### Вариант 1: Постепенная миграция (рекомендуется)

**Этап 1: Подключить API к существующему фронтенду**
- API роуты Next.js работают параллельно
- Фронтенд на Vite постепенно переходит на API вместо localStorage
- Ничего не ломается, можно откатиться

**Этап 2: Миграция страниц**
- Мигрировать страницы по одной на Next.js
- Каждая страница тестируется отдельно

**Этап 3: Полный переход**
- Удалить старый код Vite
- Оставить только Next.js

### Вариант 2: Гибридный (Vite + Next.js API)

- Фронтенд остается на Vite
- API работает на Next.js
- Оба деплоятся на Vercel

## 🔄 Что нужно сделать сейчас

### 1. Обновить vercel.json для Next.js

Уже создан новый `vercel.json` с поддержкой Next.js

### 2. Интегрировать API с существующим Quiz.tsx

Обновить `src/pages/Quiz.tsx`, чтобы:
- При сохранении ответов отправлял их на `/api/questionnaire/answers`
- При загрузке анкеты загружал из `/api/questionnaire/active`

### 3. Обновить Home.tsx для работы с API

Обновить `src/pages/Home.tsx`, чтобы:
- Загружал рекомендации из `/api/recommendations`
- Сохранял состояние выполнения в БД (опционально)

### 4. Тестирование

- Протестировать локально
- Протестировать на Vercel
- Убедиться, что ничего не сломалось

## ⚠️ Важные моменты

1. **Существующий функционал должен работать** - нельзя ломать то, что уже работает
2. **Постепенная миграция** - лучше по шагам, чем всё сразу
3. **Backward compatibility** - старые данные из localStorage должны работать

## 📝 Текущий статус

- ✅ База данных настроена
- ✅ API роуты созданы
- ✅ Анкета перенесена в БД
- ⏳ Нужно интегрировать API с существующим фронтендом
- ⏳ Обновить Quiz.tsx для работы с API
- ⏳ Обновить Home.tsx для работы с API

## 🚀 Следующие шаги

1. Обновить `src/pages/Quiz.tsx` - добавить загрузку анкеты из API
2. Обновить `src/pages/Quiz.tsx` - добавить сохранение ответов через API
3. Обновить `src/pages/Home.tsx` - загрузка рекомендаций из API
4. Протестировать локально
5. Задеплоить на Vercel
