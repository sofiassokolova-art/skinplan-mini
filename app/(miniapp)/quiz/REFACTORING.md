# Рефакторинг Quiz Page

## ✅ Выполнено

### 1. Улучшена типизация
- ✅ Создан `lib/quiz/types.ts` с строгими типами:
  - `QuestionType` enum вместо `string`
  - `QuizView` type для state machine
  - Улучшенные интерфейсы `Question`, `Questionnaire`, `SavedProgress`

### 2. Вынесены UI компоненты
- ✅ `FixedCTA` - переиспользуемая фиксированная кнопка
- ✅ `Spinner` - единый компонент загрузки
- ✅ `ErrorBlock` - блок ошибки с retry
- ✅ `BackButton` - кнопка "Назад"
- ✅ `LoaderScreen` - экран загрузки

### 3. Созданы экраны (screens/)
- ✅ `ResumeScreen.tsx` - экран "Продолжить анкету"
- ✅ `RetakeScreen.tsx` - экран выбора тем для перепрохождения
- ⏳ `InfoScreen.tsx` - информационные экраны между вопросами (renderInfoScreen очень большой, требует дальнейшего рефакторинга)
- ⏳ `QuestionScreen.tsx` - экран вопроса (требует вынесения логики из page.tsx)

### 4. Вынесены хуки ✅
- ✅ `useQuizState` - управление состоянием анкеты
- ✅ `useQuizAutoSubmit` - автоматическая отправка ответов
- ✅ `useQuizProgress` - управление прогрессом анкеты
- ✅ `useQuizRetake` - логика перепрохождения анкеты
- ✅ `useQuizInit` - объединение логики инициализации
- ✅ `useResumeLogic` - логика экрана "Продолжить анкету"

## 📋 Следующие шаги

### 5. Рефакторинг page.tsx
- [ ] Использовать state machine (`useQuizStateMachine`)
- [ ] Заменить inline styles на компоненты
- [ ] Использовать вынесенные экраны
- [ ] Упростить условия рендера через `switch (view)`

## 🎯 Приоритеты

1. **Критично**: Разбить page.tsx на экраны (снизит риск в 5-7 раз)
2. **Важно**: Использовать state machine вместо if-else спагетти
3. **Желательно**: CSS Modules или Tailwind вместо inline styles

## 📝 Заметки

- State machine уже создана в `lib/quiz/quiz-state-machine.ts`
- Хуки частично вынесены в `lib/quiz/hooks/`
- Компоненты созданы в `app/(miniapp)/quiz/components/`

