# Предложения по улучшению логики приложения

## 🔴 Критические улучшения

### 1. Загрузка продуктов для Plan28
**Проблема:** При использовании `plan28` продукты передаются только как ID, но полные данные не загружаются из БД.

**Решение:** 
- В `app/(miniapp)/plan/page.tsx` при наличии `plan28` собрать все `productId` из всех дней
- Загрузить полные данные продуктов из БД одним запросом
- Создать Map для быстрого доступа

### 2. Реализация замены продукта
**Проблема:** В `plan-client-new.tsx` есть TODO для `handleReplace` - не реализовано.

**Решение:**
- Создать API endpoint `/api/plan/replace-product` для замены продукта в плане
- Обновить `plan28.days` с новым `productId` для соответствующего `stepCategory`
- Сохранить изменения в БД (можно создать таблицу `PlanProductReplacements`)

### 3. Реализация добавления в корзину
**Проблема:** В `plan-client-new.tsx` есть TODO для `handleAddToCart` - не реализовано.

**Решение:**
- Использовать существующий API `api.addToCart(productId)`
- Добавить обработку ошибок и успешных операций

### 4. Обработка отсутствующих продуктов
**Проблема:** Если продукт удален из БД или стал неактивным, план может сломаться.

**Решение:**
- При загрузке продуктов проверять их существование
- Если продукт не найден - использовать альтернативу из `alternatives`
- Если альтернатив нет - показывать fallback продукт для этого `stepCategory`

## 🟡 Важные улучшения

### 5. Валидация данных при частичном обновлении анкеты
**Проблема:** В `partial-update/route.ts` нет валидации ответов перед сохранением.

**Решение:**
- Проверять, что `questionId` существует в анкете
- Валидировать формат ответов (single_choice vs multi_choice)
- Проверять, что выбранные варианты существуют в `answerOptions`

### 6. Автоматическая пересборка плана после обновления профиля
**Проблема:** После частичного обновления анкеты план пересобирается только если `needsPlanRebuild = true`, но логика может быть улучшена.

**Решение:**
- После обновления профиля автоматически вызывать `/api/plan/generate`
- Сохранять новый план в БД
- Уведомлять пользователя о пересборке

### 7. Кэширование продуктов
**Проблема:** Продукты загружаются каждый раз при генерации плана.

**Решение:**
- Использовать Redis для кэширования продуктов по `stepCategory`
- Инвалидировать кэш при обновлении продуктов в админке
- Кэшировать на 1 час

### 8. Улучшение логики определения weekly focus days
**Проблема:** В `plan-types.ts` `isWeeklyFocusDay` использует фиксированные дни [3, 10, 17, 24].

**Решение:**
- Учитывать `routineComplexity` из профиля
- Для `minimal` - 1 раз в неделю
- Для `medium` - 2 раза в неделю
- Для `maximal` - 3-4 раза в неделю

### 9. Обработка прогресса выполнения дня
**Проблема:** В `plan-client-new.tsx` `completedMorning` и `completedEvening` не синхронизируются с БД.

**Решение:**
- Сохранять детальный прогресс (утро/вечер отдельно) в `PlanProgress`
- Добавить поля `completedMorningDays` и `completedEveningDays`
- Обновлять API `/api/plan/progress` для поддержки детального прогресса

### 10. Оптимизация загрузки данных
**Проблема:** При загрузке плана делается много отдельных запросов.

**Решение:**
- Создать единый endpoint `/api/plan/full` который возвращает:
  - Plan28
  - Все продукты
  - Прогресс
  - Wishlist
  - Профиль
- Использовать Promise.all для параллельной загрузки

## 🟢 Улучшения UX

### 11. Предзаполнение ответов в мини-анкете
**Проблема:** В `/quiz/update/[topicId]` ответы предзаполняются, но можно улучшить UX.

**Решение:**
- Показывать визуально, что ответы предзаполнены
- Добавить кнопку "Оставить как есть" для быстрого сохранения

### 12. Индикация изменений в плане
**Проблема:** После перепрохождения анкеты пользователь не видит, что именно изменилось в плане.

**Решение:**
- Показывать diff: какие продукты добавлены/удалены/заменены
- Выделять новые шаги в плане
- Добавить объяснение изменений

### 13. Обработка ошибок сети
**Проблема:** При потере соединения нет retry-логики.

**Решение:**
- Добавить retry для критичных операций (сохранение прогресса)
- Показывать offline-режим
- Кэшировать локально и синхронизировать при восстановлении

## 📊 Производительность

### 14. Оптимизация генерации плана
**Проблема:** Генерация плана может быть медленной при большом количестве продуктов.

**Решение:**
- Использовать индексы в БД для быстрого поиска продуктов
- Кэшировать результаты фильтрации продуктов
- Оптимизировать запросы к БД (batch loading)

### 15. Lazy loading компонентов
**Проблема:** Все компоненты загружаются сразу.

**Решение:**
- Использовать React.lazy для больших компонентов
- Загружать инфографику только при необходимости
- Оптимизировать bundle size

## 🔒 Безопасность

### 16. Валидация initData
**Проблема:** initData проверяется, но можно улучшить валидацию.

**Решение:**
- Добавить проверку подписи initData на сервере
- Проверять срок действия initData
- Логировать подозрительные запросы

### 17. Rate limiting для API
**Проблема:** Нет защиты от злоупотреблений.

**Решение:**
- Добавить rate limiting для всех API endpoints
- Использовать Redis для хранения счетчиков
- Блокировать подозрительные IP

