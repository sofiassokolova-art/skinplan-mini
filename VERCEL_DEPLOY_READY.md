# 🚀 Готово к деплою на Vercel!

## ✅ Что сделано

1. ✅ Исправлена конфигурация Next.js (`next.config.mjs`)
2. ✅ Исправлены ошибки типизации в API роутах
3. ✅ Настроен build для Next.js
4. ✅ Интегрирован API в Quiz.tsx

## 📋 Важно перед деплоем

### 1. Переменные окружения на Vercel

Добавьте в **Settings → Environment Variables**:

- **DATABASE_URL** - строка подключения к Neon
- **TELEGRAM_BOT_TOKEN** - `8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw`  
- **JWT_SECRET** - сгенерированный ключ (минимум 32 символа)

### 2. Структура проекта

**Важно:** У вас гибридная структура:
- **Фронтенд Vite**: `src/pages/` (старый, работает)
- **API Next.js**: `app/api/` (новый)

Vercel будет использовать Next.js для API, а старый фронтенд продолжит работать через существующую конфигурацию.

### 3. Build команда

На Vercel используется: `npm run build` (Next.js)

## 🚀 Команды для деплоя

### Вариант 1: Через Vercel Dashboard

1. Зайдите на https://vercel.com/dashboard
2. Выберите проект `skinplan-mini`
3. Нажмите **Deploy** или просто сделайте `git push`

### Вариант 2: Через Vercel CLI

```bash
# Логин (если еще не залогинены)
npx vercel login

# Деплой в preview
npx vercel

# Деплой в production
npx vercel --prod
```

### Вариант 3: Через Git (автоматически)

```bash
git add .
git commit -m "Add Next.js API integration"
git push origin main
```

Vercel автоматически задеплоит при push.

## ✅ После деплоя проверьте

1. API работает: `https://your-app.vercel.app/api/questionnaire/active`
2. Старые страницы работают: `https://your-app.vercel.app/quiz`
3. В Telegram мини-апп открывается и работает

## ⚠️ Если возникнут проблемы

1. Проверьте логи на Vercel Dashboard
2. Убедитесь, что все переменные окружения добавлены
3. Проверьте, что база данных заполнена

## 📝 Примечание

Фронтенд на Vite (`src/pages/`) и API на Next.js (`app/api/`) работают параллельно. Vercel автоматически определит Next.js как основной фреймворк, но старые страницы продолжат работать через существующую конфигурацию.

