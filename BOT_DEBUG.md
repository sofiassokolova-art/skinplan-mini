# Отладка проблемы: Бот не отвечает на /start

## Быстрая диагностика

### 1. Проверьте статус webhook

Откройте в браузере:
```
https://skinplan-mini.vercel.app/api/telegram/webhook?action=check
```

Или через страницу админки:
```
https://skinplan-mini.vercel.app/admin/webhook-status
```

**Ожидаемый результат:**
```json
{
  "ok": true,
  "result": {
    "url": "https://skinplan-mini.vercel.app/api/telegram/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

**Если `url` пустой** — webhook не установлен.

### 2. Установите webhook

Если webhook не установлен, установите его:

**Через браузер:**
```
https://skinplan-mini.vercel.app/api/telegram/webhook?action=set-webhook
```

**Через страницу админки:**
1. Откройте `/admin/webhook-status`
2. Нажмите кнопку "Установить webhook"

**Через curl (если нужно вручную):**
```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://skinplan-mini.vercel.app/api/telegram/webhook&allowed_updates=[\"message\"]"
```

### 3. Проверьте переменные окружения в Vercel

Убедитесь, что в Vercel установлены:
- ✅ `TELEGRAM_BOT_TOKEN` — токен бота от @BotFather
- ✅ `NEXT_PUBLIC_MINI_APP_URL` — URL мини-приложения (по умолчанию: `https://skinplan-mini.vercel.app`)

### 4. Проверьте логи

1. Откройте Vercel Dashboard
2. Перейдите в раздел "Logs"
3. Отфильтруйте по `/api/telegram/webhook`
4. Отправьте `/start` боту
5. Проверьте, приходят ли логи от Telegram

**Ожидаемые логи:**
```
📥 Получено обновление от Telegram: { updateId: ..., hasMessage: true, messageText: '/start', ... }
📨 Processing /start command from user ...
📤 Sending welcome message to chat ...
✅ Welcome message sent successfully
```

### 5. Проверьте правильность токена бота

Проверьте, что токен правильный:

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe"
```

**Ожидаемый результат:**
```json
{
  "ok": true,
  "result": {
    "id": ...,
    "is_bot": true,
    "first_name": "Your Bot Name",
    "username": "your_bot_username"
  }
}
```

Если получаете ошибку `401 Unauthorized` — токен неправильный.

### 6. Проверьте, что бот может отправлять сообщения

Тестовая отправка сообщения:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": <YOUR_CHAT_ID>,
    "text": "Тестовое сообщение"
  }'
```

Замените:
- `<YOUR_BOT_TOKEN>` — токен бота
- `<YOUR_CHAT_ID>` — ваш Telegram ID (можно узнать у @userinfobot)

## Возможные проблемы и решения

### Проблема: Webhook не установлен

**Решение:**
1. Установите webhook через `/admin/webhook-status` или API
2. Подождите 1-2 минуты
3. Проверьте статус снова

### Проблема: Webhook установлен, но бот не отвечает

**Проверьте:**
1. Приходят ли логи в Vercel при отправке `/start`?
2. Если нет — проверьте URL webhook (должен быть `https://`)
3. Если да — проверьте ошибки в логах

**Возможные причины:**
- Неправильный URL webhook
- Сертификат SSL недействителен
- Функция не развернута (проверьте деплой)

### Проблема: Ошибка 401 в логах

**Причина:** Неправильный `TELEGRAM_BOT_TOKEN`

**Решение:**
1. Получите новый токен от @BotFather
2. Обновите переменную окружения в Vercel
3. Передеплойте приложение

### Проблема: Webhook установлен, логи есть, но сообщение не отправляется

**Проверьте логи на ошибки отправки:**
```
❌ Failed to send welcome message: ...
```

**Возможные причины:**
- Бот заблокирован пользователем
- Проблема с форматированием сообщения (HTML теги)
- Превышен лимит запросов к Telegram API

## Быстрый тест

1. Установите webhook: `/api/telegram/webhook?action=set-webhook`
2. Проверьте статус: `/api/telegram/webhook?action=check`
3. Отправьте `/start` боту
4. Проверьте логи в Vercel
5. Если видите логи, но нет сообщения — проверьте ошибки в логах

## Дополнительная информация

- URL webhook должен быть доступен публично (HTTPS)
- Telegram проверяет сертификат SSL
- Webhook должен отвечать в течение нескольких секунд
- Можно использовать `secret_token` для дополнительной безопасности

## Контакты для поддержки

Если проблема не решается, проверьте:
1. Документацию Telegram Bot API: https://core.telegram.org/bots/api
2. Логи Vercel
3. Статус webhook через `/api/telegram/webhook?action=check`

