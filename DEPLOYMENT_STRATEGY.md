# 🚀 Стратегия деплоя - что делать дальше

## Текущая ситуация

✅ Мини-апп уже работает на Vercel:
- URL: `skinplan-mini-5j5d1ce9x-sofias-projects-abc2e7ea.vercel.app`
- Технология: Vite + React Router
- Все страницы работают
- Данные в localStorage

✅ Новая архитектура готова:
- База данных Neon настроена
- API роуты Next.js созданы
- Анкета перенесена в БД

## 🎯 Рекомендуемая стратегия

### Вариант 1: Сначала добавить API к существующему проекту (самый безопасный)

1. **Обновить vercel.json для поддержки Next.js API**
   - API роуты будут доступны по `/api/*`
   - Существующий фронтенд продолжит работать

2. **Интегрировать API в существующий код**
   - Обновить `Quiz.tsx` - добавить загрузку анкеты из API (с fallback на localStorage)
   - Обновить `Home.tsx` - добавить загрузку рекомендаций из API

3. **Протестировать**
   - Все должно работать как раньше
   - + возможность работы с БД

4. **Постепенно мигрировать**
   - Страницы по одной переводить на Next.js

### Вариант 2: Создать отдельный проект для API

1. Создать новый проект на Vercel только для API
2. Существующий фронтенд делает запросы на новый домен API
3. Постепенно мигрировать

## 📝 Что нужно сделать сейчас

### 1. Обновить vercel.json

Уже создан новый `vercel.json` для Next.js. Но нужно убедиться, что он поддерживает оба (Vite фронтенд и Next.js API).

### 2. Обновить package.json скрипты

Уже обновлены - есть `dev:vite` и `dev` (Next.js)

### 3. Интегрировать API в Quiz.tsx

Добавить функцию, которая:
- Пытается загрузить анкету из API
- Если не получается - использует существующую логику
- При сохранении - отправляет на API (опционально)

### 4. Добавить переменные окружения на Vercel

В настройках проекта Vercel добавить:
- `DATABASE_URL` (из Neon)
- `TELEGRAM_BOT_TOKEN`
- `JWT_SECRET`

## ⚠️ Важно

Vercel может автоматически определить фреймворк. Если в проекте есть и `vite.config.ts`, и `next.config.js`, нужно явно указать в `vercel.json`, какой использовать.

## 🔄 Гибридный вариант (рекомендуется для начала)

Оставить фронтенд на Vite, но добавить Next.js только для API:

1. Переименовать `next.config.js` → `next.config.api.js`
2. Создать отдельную папку `api-server/` для Next.js API
3. Или использовать Vercel Serverless Functions напрямую

Но проще всего - **постепенно мигрировать на Next.js полностью**.

## ✅ Рекомендация

**Начать с интеграции API в существующий код:**
1. Добавить в `Quiz.tsx` загрузку из `/api/questionnaire/active`
2. Добавить сохранение в `/api/questionnaire/answers`
3. Протестировать
4. Если всё ок - продолжать миграцию страниц на Next.js

Хотите, чтобы я обновил `Quiz.tsx` для работы с API?
