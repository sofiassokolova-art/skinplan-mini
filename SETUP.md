# 🚀 Пошаговая настройка проекта

## Шаг 1: Установка зависимостей Next.js

Текущий проект использует Vite, но мы добавили Next.js. Нужно установить зависимости:

```bash
# Установить зависимости Next.js
npm install next@latest @prisma/client jsonwebtoken
npm install -D prisma @types/jsonwebtoken tsx

# Если есть конфликты версий React, используйте существующие версии
```

## Шаг 2: Обновить package.json

Добавьте скрипты Next.js в существующий `package.json`:

```json
{
  "scripts": {
    "dev": "next dev",
    "dev:vite": "vite",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "seed:questionnaire": "tsx scripts/seed-questionnaire.ts",
    "seed:products": "tsx scripts/seed-products.ts",
    "seed:rules": "tsx scripts/seed-rules.ts",
    "seed:all": "tsx scripts/seed-all.ts"
  }
}
```

## Шаг 3: Настроить базу данных (Neon)

1. **Создать аккаунт на Neon:**
   - Перейдите на https://neon.tech
   - Зарегистрируйтесь (можно через GitHub)
   - Создайте новый проект

2. **Получить connection string:**
   - В дашборде Neon нажмите "Connection Details"
   - Скопируйте connection string (пример: `postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require`)

3. **Создать .env файл:**
   ```bash
   # Создать файл .env в корне проекта
   cp .env.example .env
   ```

4. **Заполнить .env:**
   ```env
   # База данных (Neon)
   DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require"
   
   # Telegram Bot Token (получить у @BotFather)
   TELEGRAM_BOT_TOKEN="your_bot_token_here"
   
   # JWT Secret (любая случайная строка)
   JWT_SECRET="your-random-secret-key-change-in-production"
   
   # Для разработки
   NODE_ENV="development"
   ```

## Шаг 4: Создать Telegram бота

1. **Открыть @BotFather в Telegram:**
   - Найдите @BotFather в Telegram
   - Отправьте `/newbot`
   - Следуйте инструкциям для создания бота

2. **Получить токен:**
   - BotFather даст вам токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
   - Добавьте токен в `.env` как `TELEGRAM_BOT_TOKEN`

## Шаг 5: Настроить TypeScript для Next.js

Создайте/обновите `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", "app/**/*", "lib/**/*", "components/**/*"],
  "exclude": ["node_modules"]
}
```

## Шаг 6: Инициализировать базу данных

```bash
# Сгенерировать Prisma Client
npm run db:generate

# Применить схему к базе данных
npm run db:push

# Заполнить начальные данные
npm run seed:all
```

## Шаг 7: Запустить проект

```bash
# Запустить dev-сервер Next.js
npm run dev
```

Откройте http://localhost:3000

## Шаг 8: Тестирование

1. **Проверить работу API:**
   - http://localhost:3000/api/questionnaire/active (должен вернуть анкету)

2. **Тестирование в Telegram:**
   - Откройте ваш бот в Telegram
   - Используйте команду `/setmenubutton` или создайте меню с кнопкой, которая открывает ваш мини-апп
   - URL мини-аппа: `https://your-domain.com` (в разработке можно использовать ngrok)

## ⚠️ Важные замечания

1. **Два проекта в одном:**
   - Текущий проект на Vite сохранен в `src/`
   - Новый проект Next.js в `app/`
   - Можно работать с обоими, но для продакшена выберите один

2. **Переменные окружения:**
   - `.env` файл не должен быть в git (уже в .gitignore)
   - Для Vercel добавьте переменные в настройках проекта

3. **База данных:**
   - Neon предоставляет бесплатный тариф
   - Connection string содержит пароль - не публикуйте его

## 🔧 Troubleshooting

### Ошибка "Cannot find module '@prisma/client'"
```bash
npm run db:generate
```

### Ошибка "DATABASE_URL is not defined"
Проверьте, что `.env` файл существует и содержит `DATABASE_URL`

### Ошибка подключения к БД
- Проверьте connection string из Neon
- Убедитесь, что используете правильный SSL режим (`?sslmode=require`)

### Telegram бот не работает
- Проверьте токен в `.env`
- Убедитесь, что используете правильный формат токена

## 📝 Следующие шаги

После успешной настройки:
1. Протестируйте заполнение анкеты
2. Проверьте получение рекомендаций
3. Настройте деплой на Vercel (опционально)

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в консоли
2. Проверьте логи базы данных в Neon
3. Убедитесь, что все переменные окружения заполнены
