# Руководство по настройке для продакшена

## ✅ Реализованные улучшения

### P0 (Критично) - ✅ Выполнено

1. ✅ **Валидация подписи Telegram initData**
   - Реализовано в `lib/telegram.ts` через `validateTelegramInitData`
   - Использует HMAC-SHA256 для проверки подписи
   - Проверка срока действия данных (24 часа)

2. ✅ **Timeout для генерации плана**
   - Добавлен таймаут 60 секунд в `app/api/plan/generate/route.ts`
   - Используется `Promise.race` для ограничения времени выполнения

3. ✅ **Обработка сетевых ошибок**
   - Создан компонент `NetworkStatus` для отображения статуса сети
   - Добавлена функция `handleNetworkError` для понятных сообщений
   - `fetchWithTimeout` для всех API запросов (30 сек по умолчанию, 60 сек для генерации плана)

4. ✅ **Проверка переменных окружения**
   - Создан `lib/env-check.ts` с валидацией обязательных переменных
   - Автоматическая проверка при старте приложения

5. ✅ **Убраны console.log из production**
   - Все логи теперь только в development режиме

6. ✅ **Исправлено обновление User.lastActive**
   - Обновляется при каждом запросе пользователя

### P1 (Важно) - Опционально

#### 7. Мониторинг (Sentry)

**Установка:**
```bash
npm install @sentry/nextjs
```

**Настройка:**
1. Создать проект в Sentry (https://sentry.io)
2. Получить DSN
3. Добавить переменные окружения:
   ```env
   SENTRY_DSN=your-sentry-dsn
   NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn
   ```
4. Инициализировать Sentry в `sentry.client.config.ts` и `sentry.server.config.ts`
5. Интегрировать в `components/ErrorBoundary.tsx`:
   ```typescript
   import * as Sentry from '@sentry/nextjs';
   
   componentDidCatch(error: Error, errorInfo: any) {
     Sentry.captureException(error, { contexts: { react: errorInfo } });
   }
   ```

**Преимущества:**
- Отслеживание ошибок в реальном времени
- Stack traces с контекстом
- Метрики производительности

#### 8. Оптимизация изображений (Next.js Image)

**Замена обычных `<img>` на `<Image>`:**

1. В `components/CareRoutine.tsx`:
   ```typescript
   import Image from 'next/image';
   
   // Заменить:
   <img src={product.imageUrl} alt={product.name} />
   // На:
   <Image 
     src={product.imageUrl || '/placeholder.png'} 
     alt={product.name}
     width={64}
     height={64}
     style={{ borderRadius: '8px', objectFit: 'cover' }}
   />
   ```

2. В `components/WishlistItem.tsx` - аналогично

3. Настроить `next.config.mjs`:
   ```javascript
   images: {
     remotePatterns: [
       {
         protocol: 'https',
         hostname: 'your-image-host.com',
       },
     ],
   },
   ```

**Преимущества:**
- Автоматическая оптимизация изображений
- Lazy loading
- WebP формат с fallback
- Responsive images

#### 9. Кэширование на клиенте (React Query)

**Установка:**
```bash
npm install @tanstack/react-query
```

**Настройка:**
1. Создать `providers/QueryProvider.tsx`:
   ```typescript
   'use client';
   import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
   
   const queryClient = new QueryClient({
     defaultOptions: {
       queries: {
         staleTime: 5 * 60 * 1000, // 5 минут
         cacheTime: 10 * 60 * 1000, // 10 минут
       },
     },
   });
   
   export function QueryProvider({ children }) {
     return (
       <QueryClientProvider client={queryClient}>
         {children}
       </QueryClientProvider>
     );
   }
   ```

2. Обернуть приложение в `app/layout.tsx`:
   ```typescript
   import { QueryProvider } from '@/providers/QueryProvider';
   
   <QueryProvider>
     {children}
   </QueryProvider>
   ```

3. Использовать в компонентах:
   ```typescript
   import { useQuery } from '@tanstack/react-query';
   
   const { data, isLoading, error } = useQuery({
     queryKey: ['plan', userId],
     queryFn: () => api.getPlan(),
   });
   ```

**Преимущества:**
- Автоматическое кэширование запросов
- Фоновое обновление данных
- Retry логика
- Оптимистичные обновления

## 📋 Чек-лист перед деплоем

### Обязательно

- [x] Переменные окружения настроены на Vercel
- [x] Health check endpoint работает (`/api/health`)
- [x] Все критичные улучшения реализованы
- [x] Тесты на основных функциях (если есть)

### Желательно

- [ ] Sentry настроен для мониторинга
- [ ] Изображения оптимизированы через Next.js Image
- [ ] Кэширование на клиенте через React Query
- [ ] Настроены алерты в Sentry/Vercel
- [ ] Проведено нагрузочное тестирование

## 🔐 Безопасность

### Обязательные переменные окружения

```env
# База данных
DATABASE_URL=postgresql://...

# Telegram
TELEGRAM_BOT_TOKEN=...

# JWT
JWT_SECRET=...

# Опционально
KV_REST_API_URL=...
KV_REST_API_TOKEN=...
UPSTASH_REDIS_REST_URL=...
UPSTASH_REDIS_REST_TOKEN=...
```

### Проверка безопасности

- [x] Валидация подписи Telegram initData
- [x] Rate limiting настроен
- [x] CORS настроен
- [ ] Security headers (HSTS, CSP и т.д.)
- [ ] SQL injection защита (Prisma автоматически защищает)

## 📊 Мониторинг

### Метрики для отслеживания

1. **Производительность:**
   - Время генерации плана
   - Время загрузки страниц
   - Размер ответов API

2. **Ошибки:**
   - Количество ошибок по типам
   - Частота ошибок
   - Stack traces

3. **Использование:**
   - Количество активных пользователей
   - Количество прохождений анкеты
   - Количество генераций плана

## 🚀 Деплой

1. Проверить, что все переменные окружения установлены на Vercel
2. Проверить health check: `https://your-app.vercel.app/api/health`
3. Протестировать основные сценарии:
   - Прохождение анкеты
   - Генерация плана
   - Добавление в избранное
   - Добавление в корзину

## 📝 Документация

- `PRODUCTION_IMPROVEMENTS.md` - полный список улучшений
- `DB_REVIEW.md` - ревью базы данных
- `PRODUCTION_SETUP_GUIDE.md` - это руководство

