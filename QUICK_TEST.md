# ⚡ Быстрый тест интеграции

## 🚀 Шаг 1: Запустите сервер

```bash
npm run dev
```

Должен запуститься на http://localhost:3000

## 🧪 Шаг 2: Проверьте API в браузере

### 1. Проверка анкеты
Откройте в браузере:
```
http://localhost:3000/api/questionnaire/active
```

Должен вернуть JSON с анкетой (21 вопрос, 7 групп)

### 2. Проверка Quiz страницы
Откройте:
```
http://localhost:3000/quiz
```

Должна открыться страница анкеты

## 📱 Шаг 3: Тестирование в Telegram (опционально)

1. Задеплойте на Vercel (или используйте локальный туннель)
2. Откройте мини-апп в Telegram боте
3. Пройдите анкету
4. Проверьте консоль браузера (F12) на ошибки

## ✅ Что проверить

### В консоли браузера (F12):
- [ ] Нет критических ошибок
- [ ] API запросы отправляются (Network tab)
- [ ] Токен сохраняется в localStorage (если в Telegram)
- [ ] Ответы сохраняются в localStorage

### В базе данных:
- [ ] Пользователь создается при авторизации
- [ ] Ответы сохраняются в таблицу `user_answers`
- [ ] Профиль кожи создается в `skin_profiles`

## 🐛 Если что-то не работает

1. **API не отвечает:**
   - Проверьте, что сервер запущен: `npm run dev`
   - Проверьте .env файл (DATABASE_URL должен быть)

2. **Ошибка авторизации:**
   - Проверьте TELEGRAM_BOT_TOKEN в .env
   - Проверьте, что токен правильный

3. **Анкета не загружается:**
   - Проверьте, что данные заполнены: `npm run seed:questionnaire-full`
   - Проверьте базу данных: `npm run db:studio`

4. **Ответы не сохраняются:**
   - Проверьте консоль на ошибки
   - Проверьте, что есть токен в localStorage (если открыто в Telegram)
   - Если нет токена - ответы сохраняются только в localStorage (это нормально)

## 📝 Логи для отладки

Откройте консоль браузера (F12) и ищите:
- ✅ "Telegram auth successful" - авторизация прошла
- ✅ "Answers saved to API successfully" - ответы сохранены
- ⚠️ "No auth token, skipping API save" - нормально, если не в Telegram
- ❌ Любые ошибки (красные сообщения)

