# ✅ Готово к деплою!

## ⚠️ Важное решение по структуре

Для избежания конфликта Next.js (видит и `app`, и `src/pages`):
- **`src/pages`** временно переименован в **`src/_pages_vite`**
- Это позволяет Next.js собирать только API роуты

**После деплоя** можно вернуть обратно через скрипт.

## 🚀 Деплой на Vercel

### Вариант 1: Через Git (рекомендуется)

```bash
git add .
git commit -m "Prepare for deployment with Next.js API"
git push origin main
```

Vercel автоматически задеплоит.

### Вариант 2: Через Vercel CLI

```bash
npx vercel --prod
```

## ⚙️ Переменные окружения на Vercel

Убедитесь, что добавлены:
- **DATABASE_URL** - строка подключения Neon
- **TELEGRAM_BOT_TOKEN** - `8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw`
- **JWT_SECRET** - секретный ключ

## 📝 После деплоя

1. Проверьте API: `https://your-app.vercel.app/api/questionnaire/active`
2. Проверьте старые страницы (через Vite фронтенд)
3. Откройте мини-апп в Telegram

