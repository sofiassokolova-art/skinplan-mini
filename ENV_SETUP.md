# 🔐 Настройка переменных окружения

## Токен Telegram бота

Ваш токен получен. Нужно добавить его в `.env` файл.

**Токен бота:**
```
8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw
```

## 📝 Что нужно сделать

### 1. Локально (в файле `.env`)

Откройте файл `.env` в корне проекта и добавьте/обновите:

```env
TELEGRAM_BOT_TOKEN="8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw"
```

### 2. На Vercel (в настройках проекта)

1. Зайдите в проект на Vercel: https://vercel.com/dashboard
2. Выберите проект `skinplan-mini`
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте переменную:
   - **Key:** `TELEGRAM_BOT_TOKEN`
   - **Value:** `8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw`
   - **Environment:** Production, Preview, Development (все)

## ✅ Проверка

После добавления токена:
1. Перезапустите локальный сервер (`npm run dev`)
2. Проверьте, что API роут `/api/auth/telegram` работает
3. В логах не должно быть ошибки "Bot token not configured"

## 📋 Все необходимые переменные

Убедитесь, что в `.env` есть все переменные:

```env
# Database (уже настроена)
DATABASE_URL="postgresql://..."

# Telegram Bot (добавьте сейчас)
TELEGRAM_BOT_TOKEN="8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw"

# JWT Secret (должен быть сгенерирован)
JWT_SECRET="..."

# OpenAI API Key (опционально, для daily advice)
OPENAI_API_KEY="sk-..."
```

## 🔒 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` в Git (уже в `.gitignore`)
- Не публикуйте токен в открытом доступе
- Если токен скомпрометирован - создайте новый через @BotFather

