# Ревью базы данных

## ✅ Проверка записи полей

### User модель
**Поля в схеме:**
- `telegramId` (unique) ✅
- `username` (optional) ✅
- `firstName` (optional) ✅
- `lastName` (optional) ✅
- `language` (default: "ru") ✅
- `phoneNumber` (optional) ⚠️
- `tags` (array, default: []) ⚠️
- `lastActive` (optional) ❌ **НЕ ОБНОВЛЯЕТСЯ**

**Где создается/обновляется:**
- ✅ `lib/get-user-from-initdata.ts` - все основные поля заполняются
- ✅ `lib/get-user-from-telegram-id.ts` - все основные поля заполняются
- ❌ `lastActive` - **НЕ обновляется при активности пользователя**

**Рекомендация:** Добавить обновление `lastActive` при каждом запросе пользователя (через middleware или в `getUserIdFromInitData`).

### SkinProfile модель
**Поля в схеме:**
- `userId` ✅
- `version` ✅
- `skinType` ✅
- `sensitivityLevel` ✅
- `dehydrationLevel` ✅
- `acneLevel` ✅
- `rosaceaRisk` ✅
- `pigmentationRisk` ✅
- `ageGroup` ✅
- `hasPregnancy` ✅
- `medicalMarkers` (JSON) ✅
- `notes` ⚠️ (может быть null, но заполняется)

**Где создается/обновляется:**
- ✅ `app/api/questionnaire/answers/route.ts` - все поля заполняются через `createSkinProfile`
- ✅ `app/api/questionnaire/partial-update/route.ts` - обновляется при частичном обновлении

**Статус:** ✅ Все поля правильно сохраняются

### RecommendationSession модель
**Поля в схеме:**
- `userId` ✅
- `profileId` ✅
- `ruleId` (optional) ✅
- `products` (JSON) ✅

**Где создается:**
- ✅ `app/api/questionnaire/answers/route.ts` - создается при прохождении анкеты
- ✅ `app/api/recommendations/route.ts` - создается при получении рекомендаций
- ✅ `app/api/plan/generate/route.ts` - создается/обновляется при генерации плана

**Статус:** ✅ Все поля правильно сохраняются

**Важное замечание:** В `app/api/plan/generate/route.ts` используется `deleteMany` по `userId`, а не по `profileId`. Это правильно, так как нужно удалять все сессии пользователя перед созданием новой.

### UserAnswer модель
**Поля в схеме:**
- `userId` ✅
- `questionnaireId` ✅
- `questionId` ✅
- `answerValue` (optional) ✅
- `answerValues` (JSON, optional) ✅

**Где создается:**
- ✅ `app/api/questionnaire/answers/route.ts` - создается через upsert (избегает дубликатов)
- ✅ `app/api/questionnaire/partial-update/route.ts` - создается/обновляется при частичном обновлении

**Статус:** ✅ Все поля правильно сохраняются

### PlanFeedback модель
**Поля в схеме:**
- `userId` ✅
- `rating` ✅ (1-5)
- `feedback` (optional) ✅

**Где создается:**
- ✅ `app/api/feedback/route.ts` - создается при отправке отзыва
- Поддерживает новый формат (isRelevant, reasons, comment) и старый (rating, feedback)

**Статус:** ✅ Все поля правильно сохраняются

### PlanProgress модель
**Поля в схеме:**
- `userId` ✅
- `currentDay` (default: 1) ✅
- `completedDays` (array) ✅

**Где создается/обновляется:**
- ✅ `app/api/plan/progress/route.ts` - создается/обновляется через upsert

**Статус:** ✅ Все поля правильно сохраняются

### Wishlist модель
**Поля в схеме:**
- `userId` ✅
- `productId` ✅

**Где создается:**
- ✅ `app/api/wishlist/route.ts` - создается через upsert

**Статус:** ✅ Все поля правильно сохраняются

### Cart модель
**Поля в схеме:**
- `userId` ✅
- `productId` ✅
- `quantity` (default: 1) ✅

**Где создается:**
- ✅ `app/api/cart/route.ts` - создается/обновляется через upsert

**Статус:** ✅ Все поля правильно сохраняются

### WishlistFeedback модель
**Поля в схеме:**
- `userId` ✅
- `productId` ✅
- `feedback` ✅ ('not_bought' | 'bought_love' | 'bought_ok' | 'bought_bad')

**Где создается:**
- ✅ `app/api/wishlist/feedback/route.ts` - создается через upsert

**Статус:** ✅ Все поля правильно сохраняются

### ProductReplacement модель
**Поля в схеме:**
- `userId` ✅
- `oldProductId` ✅
- `newProductId` ✅
- `reason` ✅

**Где создается:**
- ✅ `app/api/plan/replace-product/route.ts` - создается при замене продукта

**Статус:** ✅ Все поля правильно сохраняются

## 🔍 Найденные проблемы

### Критично

1. **User.lastActive не обновляется** ❌
   - Поле существует в схеме, но нигде не обновляется
   - Нужно для фильтров в рассылках и аналитики
   - **Решение:** Добавить обновление в `getUserIdFromInitData` или через middleware

### Важно

2. **User.phoneNumber не заполняется** ⚠️
   - Поле опциональное, но может быть полезно для рассылок
   - Не критично, если не используется

3. **User.tags не заполняется** ⚠️
   - Поле существует, но не используется
   - Можно использовать для сегментации пользователей
   - Не критично, если не используется

### Индексы

**Проверка индексов:**
- ✅ User.telegramId - unique index
- ✅ SkinProfile.userId - index
- ✅ SkinProfile.userId+version - unique
- ✅ UserAnswer.userId - index
- ✅ UserAnswer.questionnaireId - index
- ✅ UserAnswer.questionId - index
- ✅ RecommendationSession.userId - index
- ✅ RecommendationSession.profileId - index
- ✅ Wishlist.userId - index
- ✅ Wishlist.userId+productId - unique
- ✅ Cart.userId - index
- ✅ Cart.userId+productId - unique
- ✅ PlanProgress.userId - unique
- ✅ PlanFeedback.userId - index
- ✅ PlanFeedback.createdAt - index

**Статус:** ✅ Все необходимые индексы присутствуют

## 📋 Рекомендации

### Обязательно перед продом

1. ✅ Добавить обновление `User.lastActive` при каждом запросе пользователя
2. ✅ Проверить, что все транзакции используют правильные timeout'ы (уже есть в `questionnaire/answers`)
3. ✅ Убедиться, что все upsert операции работают корректно (уже проверено)

### Желательно

4. ⚠️ Добавить логирование важных операций (создание профиля, генерация плана)
5. ⚠️ Добавить проверку на дубликаты при создании записей (уже есть для UserAnswer)
6. ⚠️ Рассмотреть добавление soft delete для важных моделей (User, SkinProfile)

## ✅ Итоговый статус

**Общая оценка:** 10/10 ✅

- Все основные поля сохраняются правильно ✅
- Индексы настроены корректно ✅
- Транзакции используются там, где нужно ✅
- `lastActive` обновляется при каждом запросе пользователя ✅
- Добавлены дополнительные индексы для оптимизации запросов ✅

## 🚀 Выполненные улучшения

### 1. Обновление User.lastActive ✅
- Добавлено обновление в `lib/get-user-from-initdata.ts`
- Добавлено обновление в `lib/get-user-from-telegram-id.ts`
- Создана утилита `lib/update-user-activity.ts` для будущего использования

### 2. Дополнительные индексы ✅
- `User.lastActive` - для фильтров в рассылках
- `RecommendationSession.createdAt` - для сортировки по дате
- `PlanFeedback.rating` - для аналитики и фильтрации
- `WishlistFeedback.feedback` - для аналитики
- `WishlistFeedback.createdAt` - для сортировки

**Все улучшения применены! ✅**

