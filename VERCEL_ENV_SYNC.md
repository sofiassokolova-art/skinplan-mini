# 🔄 Синхронизация переменных окружения из Vercel

У вас уже есть переменные окружения на Vercel! Вот как их использовать локально:

## Способ 1: Через Vercel CLI (рекомендуется)

### 1. Установить Vercel CLI (если еще нет)
```bash
npm install -g vercel
```

### 2. Войти в Vercel
```bash
vercel login
```

### 3. Привязать проект (если еще не привязан)
```bash
vercel link
```

### 4. Скачать переменные окружения
```bash
vercel env pull .env.local
```

Или просто в `.env`:
```bash
vercel env pull .env
```

Это скачает все переменные из Vercel в локальный файл!

## Способ 2: Вручную скопировать из Vercel Dashboard

1. Зайдите на https://vercel.com
2. Откройте ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Скопируйте каждую переменную:
   - `DATABASE_URL`
   - `TELEGRAM_BOT_TOKEN`
   - `JWT_SECRET`
5. Вставьте их в локальный файл `.env`

## Способ 3: Использовать .env.local (Next.js автоматически подхватит)

Если у вас уже есть переменные на Vercel, можно создать `.env.local`:

```bash
vercel env pull .env.local
```

Next.js автоматически использует `.env.local` в разработке.

## ✅ После синхронизации

После того как переменные скопированы, можно запускать:

```bash
npm run db:generate
npm run db:push
npm run seed:all
npm run dev
```

---

**Примечание:** `.env.local` не попадает в git (уже в .gitignore), так что безопасно хранить там секреты.
