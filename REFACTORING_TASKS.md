# Задачи по рефакторингу quiz/page.tsx

## ✅ Выполнено

1. ✅ Вынесен `QuizQuestion` - компонент для рендеринга вопроса
2. ✅ Вынесен `QuizInfoScreen` - компонент для рендеринга инфо-экранов
3. ✅ Вынесены `QuizErrorDisplay` и `QuizFinalizingLoader` - компоненты для ошибок и лоадеров
4. ✅ Вынесены `handleAnswer` и `handleBack` - обработчики ответов и навигации назад
5. ✅ Вынесен `submitAnswers` - функция отправки ответов (~1268 строк)
6. ✅ Вынесен `resumeQuiz` - функция восстановления прогресса (~208 строк)
7. ✅ Вынесен `startOver` - функция начала анкеты заново (~135 строк)

**Текущий размер:** 5502 строки (было ~8253 строки)
**Уменьшение:** ~2751 строка (-33.3%)

---

## 📋 Оставшиеся задачи

### Приоритет 1: Вынести функции работы с прогрессом

#### 1. Вынести `saveProgress` в `lib/quiz/handlers/saveProgress.ts`
- **Текущее местоположение:** строки ~2053-2123
- **Размер:** ~70 строк
- **Зависимости:** 
  - `setSavedProgress`, `setShowResumeScreen`, `hasResumedRef`, `setHasResumed`
  - `lastSavedAnswerRef`, `saveProgressTimeoutRef`, `pendingProgressRef`
  - `api.saveQuizProgress()`, `QUIZ_CONFIG`, `clientLogger`
- **Интерфейс:** `SaveProgressParams`

#### 2. Вынести `clearProgress` в `lib/quiz/handlers/clearProgress.ts`
- **Текущее местоположение:** строки ~2127-2157
- **Размер:** ~30 строк
- **Зависимости:**
  - `setSavedProgress`, `setShowResumeScreen`, `hasResumedRef`, `setHasResumed`
  - `lastSavedAnswerRef`, `api.clearQuizProgress()`, `QUIZ_CONFIG`, `clientLogger`
- **Интерфейс:** `ClearProgressParams`

**Ожидаемое уменьшение:** ~100 строк

---

### Приоритет 2: Создать хуки для группировки состояния

#### 3. Создать `useQuizState` - хук для управления состоянием
- **Что вынести:**
  - Все `useState` (questionnaire, loading, error, currentQuestionIndex, currentInfoScreenIndex, answers, showResumeScreen, isSubmitting, finalizing, finalizingStep, finalizeError, pendingInfoScreen, savedProgress, isRetakingQuiz, showRetakeScreen, hasResumed)
  - Все `useRef` (questionnaireRef, currentInfoScreenIndexRef, isSubmittingRef, и т.д.)
  - Синхронизацию ref с state через useEffect
- **Файл:** `lib/quiz/hooks/useQuizState.ts`
- **Возвращает:** объект со всеми состояниями и сеттерами

**Ожидаемое уменьшение:** ~200-300 строк

#### 4. Создать `useQuizProgress` - хук для управления прогрессом
- **Что вынести:**
  - Логику сохранения и загрузки прогресса
  - Синхронизацию с React Query
  - Дебаунсинг для сохранения
- **Файл:** `lib/quiz/hooks/useQuizProgress.ts`
- **Примечание:** Частично уже есть в `hooks/useQuiz.ts`, но нужно расширить

**Ожидаемое уменьшение:** ~150-200 строк

---

### Приоритет 3: Создать хуки для группировки эффектов

#### 5. Создать `useQuizEffects` - хук для группировки useEffect
- **Что вынести:**
  - Эффекты инициализации (загрузка анкеты, проверка Telegram)
  - Эффекты синхронизации (questionnaire, answers, progress)
  - Эффекты восстановления прогресса
  - Эффекты сохранения прогресса
  - Эффекты навигации и редиректов
- **Файл:** `lib/quiz/hooks/useQuizEffects.ts`
- **Структура:** Разделить на под-хуки:
  - `useQuizInitialization` - инициализация
  - `useQuizSynchronization` - синхронизация состояний
  - `useQuizProgressSync` - синхронизация прогресса
  - `useQuizNavigation` - навигация и редиректы

**Ожидаемое уменьшение:** ~400-500 строк

---

### Приоритет 4: Оптимизация и упрощение

#### 6. Вынести вычисляемые значения в отдельные хуки
- **Что вынести:**
  - `allQuestionsRaw` и `allQuestions` - в `useQuizQuestions`
  - `currentQuestion`, `currentInfoScreen` - в `useQuizCurrentItem`
  - `effectiveAnswers`, `answersCount` - в `useQuizAnswers`
- **Файлы:**
  - `lib/quiz/hooks/useQuizQuestions.ts`
  - `lib/quiz/hooks/useQuizCurrentItem.ts`
  - `lib/quiz/hooks/useQuizAnswers.ts`

**Ожидаемое уменьшение:** ~200-300 строк

#### 7. Вынести функции инициализации
- **Что вынести:**
  - Функцию `init()` - в `lib/quiz/handlers/init.ts`
  - Логику загрузки анкеты - уже частично в `loadQuestionnaire.ts`
  - Логику проверки Telegram - можно в отдельный хук
- **Файл:** `lib/quiz/handlers/init.ts`

**Ожидаемое уменьшение:** ~150-200 строк

#### 8. Упростить основной компонент QuizPage
- **Что сделать:**
  - Оставить только композицию хуков и компонентов
  - Убрать дублирование логики
  - Упростить условный рендеринг
  - Вынести константы и конфигурацию
- **Целевой размер:** ~2000-2500 строк

**Ожидаемое уменьшение:** ~500-800 строк

---

### Приоритет 5: Дополнительные улучшения

#### 9. Создать контекст для quiz
- **Файл:** `lib/quiz/context/QuizContext.tsx`
- **Что включить:**
  - Все состояния quiz
  - Все обработчики
  - Утилиты
- **Преимущества:** Упростит передачу пропсов, уменьшит prop drilling

#### 10. Оптимизировать производительность
- Мемоизация тяжелых вычислений
- Оптимизация re-renders через React.memo
- Оптимизация зависимостей useEffect
- Использование useTransition для неблокирующих обновлений

#### 11. Улучшить типизацию
- Создать общие типы для параметров функций
- Использовать утилитные типы для уменьшения дублирования
- Добавить JSDoc комментарии

#### 12. Добавить тесты
- Unit тесты для обработчиков
- Интеграционные тесты для хуков
- E2E тесты для основного флоу

---

## 📊 Прогноз итогового результата

**Текущий размер:** 5502 строки

**После выполнения приоритетов 1-4:**
- Вынос функций: -100 строк
- useQuizState: -250 строк
- useQuizProgress: -175 строк
- useQuizEffects: -450 строк
- Вычисляемые значения: -250 строк
- Функции инициализации: -175 строк
- Упрощение компонента: -650 строк

**Итого:** ~3462 строки (-37% от текущего, -58% от исходного)

**После выполнения всех задач:**
- Целевой размер: ~2000-2500 строк
- Уменьшение: ~75% от исходного размера

---

## 🎯 Рекомендуемый порядок выполнения

1. ✅ Вынести компоненты (выполнено)
2. ✅ Вынести обработчики (выполнено)
3. ⏳ Вынести функции работы с прогрессом (saveProgress, clearProgress)
4. ⏳ Создать useQuizState
5. ⏳ Создать useQuizProgress
6. ⏳ Создать useQuizEffects
7. ⏳ Вынести вычисляемые значения
8. ⏳ Вынести функции инициализации
9. ⏳ Упростить основной компонент
10. ⏳ Дополнительные улучшения

---

## 📝 Примечания

- Все изменения должны быть пошаговыми с коммитами
- После каждого шага проверять компиляцию и работу приложения
- Сохранять обратную совместимость
- Документировать изменения в комментариях


