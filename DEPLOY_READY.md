# ✅ Готово к деплою на Vercel!

## ✅ Билд успешно завершен!

Next.js успешно собрался, все API роуты готовы:
- ✅ `/api/auth/telegram`
- ✅ `/api/questionnaire/active`
- ✅ `/api/questionnaire/answers`
- ✅ `/api/recommendations`
- ✅ `/api/profile/current`

## 🚀 Как деплоить

### Вариант 1: Через Git (самый простой)

```bash
git add .
git commit -m "Add Next.js API integration"
git push origin main
```

Vercel автоматически задеплоит проект.

### Вариант 2: Через Vercel CLI

```bash
# Если еще не установлен Vercel CLI
npm install -g vercel

# Логин
vercel login

# Деплой
vercel --prod
```

## ⚙️ Важно: Переменные окружения

Перед деплоем добавьте на Vercel в **Settings → Environment Variables**:

1. **DATABASE_URL** 
   - Значение: строка подключения к Neon базе `neon-red-yacht`
   
2. **TELEGRAM_BOT_TOKEN**
   - Значение: `8138388674:AAHt8HqnPS3LRwo7l_g_q1Bw05c9vTqsfEw`
   
3. **JWT_SECRET**
   - Значение: сгенерированный секретный ключ (минимум 32 символа)
   - Можно сгенерировать: `openssl rand -base64 32`

**Важно:** Выберите все окружения (Production, Preview, Development)

## 📋 После деплоя

1. Проверьте, что API работает:
   ```
   https://your-app.vercel.app/api/questionnaire/active
   ```

2. Проверьте старые страницы (они должны продолжать работать через существующий фронтенд)

3. Откройте мини-апп в Telegram и протестируйте

## 🔄 Если нужно вернуть src/pages

Для локальной разработки старый фронтенд:
```bash
./scripts/restore-pages.sh
```

Или просто переименуйте обратно:
```bash
mv src/_pages_vite src/pages
```

## ✅ Всё готово!

Проект готов к деплою. Можете делать `git push` или `vercel --prod`!

