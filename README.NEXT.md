# SkinIQ - Новая архитектура (Next.js)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Переименовать package.next.json в package.json
mv package.next.json package.json

# Установить зависимости
npm install
```

### 2. Настройка базы данных

1. Создать проект на [Neon](https://neon.tech) или другом PostgreSQL провайдере
2. Скопировать `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```
3. Заполнить `DATABASE_URL` в `.env`
4. Выполнить миграции:
   ```bash
   npm run db:generate
   npm run db:push
   ```

### 3. Настройка Telegram бота

1. Создать бота через [@BotFather](https://t.me/botfather)
2. Получить токен бота
3. Добавить в `.env`:
   ```
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   JWT_SECRET=your_random_secret_here
   ```

### 4. Запуск dev-сервера

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 📁 Структура проекта

```
├── app/                          # Next.js App Router
│   ├── (miniapp)/               # Мини-апп для пользователей
│   │   ├── page.tsx             # Главная (рутина)
│   │   ├── quiz/                # Анкета
│   │   └── plan/                # План ухода
│   ├── admin/                   # CMS панель (будущее)
│   └── api/                     # API роуты
│       ├── auth/
│       │   └── telegram/        # Авторизация Telegram
│       ├── questionnaire/       # Анкета
│       ├── profile/             # Профиль пользователя
│       └── recommendations/     # Рекомендации
├── lib/                         # Утилиты
│   ├── db.ts                    # Prisma Client
│   ├── telegram.ts              # Валидация Telegram
│   └── profile-calculator.ts    # Расчёт профиля
├── prisma/
│   └── schema.prisma            # Схема БД
├── components/                  # React компоненты (перенести из src/)
└── public/                      # Статические файлы
```

## 🔑 API Endpoints

### Авторизация
- `POST /api/auth/telegram` — авторизация через Telegram

### Анкета
- `GET /api/questionnaire/active` — получить активную анкету
- `POST /api/questionnaire/answers` — сохранить ответы и рассчитать профиль

### Профиль
- `GET /api/profile/current` — текущий профиль пользователя

### Рекомендации
- `GET /api/recommendations` — получить рекомендации продуктов

## 🗄️ База данных

Используется Prisma ORM. Основные таблицы:

- `users` — пользователи Telegram
- `skin_profiles` — профили кожи
- `questionnaires` — версии анкет
- `questions`, `answer_options` — структура анкеты
- `user_answers` — ответы пользователей
- `products`, `brands` — каталог продуктов
- `recommendation_rules` — правила рекомендаций
- `recommendation_sessions` — лог рекомендаций

## 🔄 Миграция с текущего проекта

Подробные инструкции см. в [MIGRATION.md](./MIGRATION.md)

## 📚 Документация

- [ARCHITECTURE.md](./ARCHITECTURE.md) — подробное описание архитектуры
- [MIGRATION.md](./MIGRATION.md) — инструкция по миграции

## ⚠️ Важные заметки

1. **Текущий проект (Vite) сохранен** — можно работать параллельно
2. **Нужно заполнить начальные данные** — анкету и продукты
3. **Админ-панель** — планируется в следующих итерациях
4. **Фото** — пока не реализовано, будет добавлено позже

## 🛠️ Скрипты

```bash
npm run dev          # Запуск dev-сервера
npm run build        # Сборка для продакшена
npm run start        # Запуск продакшен-сервера
npm run db:generate  # Генерация Prisma Client
npm run db:push      # Применить изменения схемы
npm run db:studio    # Prisma Studio (GUI для БД)
```

## 📝 Следующие шаги

1. ✅ Создать базовую структуру Next.js
2. ✅ Настроить базу данных (Prisma)
3. ✅ Создать API роуты
4. ⏳ Перенести UI компоненты
5. ⏳ Создать страницы мини-аппа
6. ⏳ Заполнить начальные данные (анкета, продукты)
7. ⏳ Создать админ-панель
