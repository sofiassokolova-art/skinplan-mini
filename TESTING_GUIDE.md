# 🧪 Руководство по тестированию

## ✅ Что было сделано

1. ✅ Интегрирован API в Quiz.tsx
2. ✅ Добавлена авторизация через Telegram
3. ✅ Добавлено сохранение ответов через API
4. ✅ Исправлена конфигурация Next.js (next.config.mjs)

## 🚀 Как запустить тестирование

### Шаг 1: Запустите сервер

В терминале выполните:
```bash
npm run dev
```

Сервер должен запуститься на **http://localhost:3000**

### Шаг 2: Проверьте API

Откройте в браузере:
```
http://localhost:3000/api/questionnaire/active
```

**Ожидаемый результат:** JSON с анкетой (21 вопрос, 7 групп)

### Шаг 3: Проверьте Quiz страницу

Откройте:
```
http://localhost:3000/quiz
```

**Ожидаемый результат:** Страница анкеты открывается

### Шаг 4: Пройдите анкету

1. Отвечайте на вопросы
2. Откройте консоль браузера (F12)
3. Проверьте:
   - Нет ошибок в Console
   - В Network tab видны запросы
   - В localStorage сохраняются ответы (Application tab → Local Storage)

### Шаг 5: Завершите анкету

1. Пройдите все вопросы
2. Нажмите "Завершить"
3. Проверьте:
   - Переход на страницу `/plan`
   - В консоли: "Answers saved to API successfully" (если есть токен)
   - Или: "No auth token, skipping API save" (если не в Telegram - это нормально)

## 📱 Тестирование в Telegram

1. **Задеплойте на Vercel:**
   ```bash
   vercel --prod
   ```

2. **Откройте мини-апп в Telegram боте**

3. **Пройдите анкету:**
   - Должна произойти авторизация
   - В консоли: "Telegram auth successful"
   - Ответы сохраняются через API

## 🔍 Что проверить

### ✅ Должно работать:

- [ ] API `/api/questionnaire/active` возвращает анкету
- [ ] Страница `/quiz` открывается
- [ ] Можно отвечать на вопросы
- [ ] Ответы сохраняются в localStorage
- [ ] После завершения переход на `/plan`
- [ ] Нет критических ошибок в консоли

### ⚠️ Ожидаемое поведение:

- Если не в Telegram - токен не будет (это нормально)
- Ответы сохраняются только в localStorage без токена
- С токеном - дополнительно сохраняются через API

### ❌ Проблемы и решения:

**API не отвечает:**
- Проверьте, что сервер запущен: `npm run dev`
- Проверьте .env файл (DATABASE_URL должен быть)
- Проверьте логи: `tail -f /tmp/next-test.log`

**Ошибка авторизации:**
- Проверьте TELEGRAM_BOT_TOKEN в .env
- Проверьте, что токен правильный

**Анкета не загружается:**
- Проверьте, что данные заполнены: `npm run seed:questionnaire-full`
- Проверьте базу данных: `npm run db:studio`

## 📝 Логи для отладки

В консоли браузера (F12 → Console) ищите:

- ✅ `"Telegram auth successful"` - авторизация прошла
- ✅ `"Answers saved to API successfully"` - ответы сохранены
- ⚠️ `"No auth token, skipping API save"` - нормально, если не в Telegram
- ❌ Любые ошибки (красные сообщения) - нужно исправить

## 🎯 Следующие шаги после тестирования

1. ✅ Убедитесь, что всё работает локально
2. ✅ Задеплойте на Vercel
3. ✅ Протестируйте в Telegram
4. ✅ Интегрируйте API в Home.tsx (TODO)

## 💡 Полезные команды

```bash
# Запустить сервер
npm run dev

# Проверить API
curl http://localhost:3000/api/questionnaire/active

# Открыть базу данных
npm run db:studio

# Заполнить данные заново
npm run seed:questionnaire-full

# Автоматический тест API
node test-api.js
```

