import React from "react";
import { createRoot } from "react-dom/client";
import "./index.css";
import App from "./App";   // ✅ работает, если в App.tsx есть export default App

createRoot(document.getElementById("root")!).render(<App />);

