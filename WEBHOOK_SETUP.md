# Установка Telegram Webhook

## Проблема
Webhook не установлен. В результате бот не отвечает на команды.

## Решение: Установите webhook

### Вариант 1: Через браузер (самый простой)

Откройте эту ссылку в браузере:
```
https://skinplan-mini.vercel.app/api/telegram/webhook?action=set-webhook
```

**Ожидаемый результат:**
```json
{
  "ok": true,
  "result": true,
  "description": "Webhook was set"
}
```

### Вариант 2: Через страницу админки

1. Откройте: `https://skinplan-mini.vercel.app/admin/webhook-status`
2. Нажмите кнопку **"Установить webhook"**
3. Дождитесь сообщения об успешной установке

### Вариант 3: Через curl (если нужно)

```bash
curl "https://skinplan-mini.vercel.app/api/telegram/webhook?action=set-webhook"
```

## После установки

### 1. Проверьте статус webhook

Откройте:
```
https://skinplan-mini.vercel.app/api/telegram/webhook?action=check
```

**Ожидаемый результат:**
```json
{
  "ok": true,
  "result": {
    "url": "https://skinplan-mini.vercel.app/api/telegram/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

После установки `pending_update_count` должен обнулиться или уменьшиться после обработки накопленных обновлений.

### 2. Проверьте работу бота

1. Откройте бота `@skinplanned_bot` в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением с кнопкой для открытия Mini App

### 3. Проверьте логи

Если бот не отвечает после установки webhook:

1. Откройте Vercel Dashboard
2. Перейдите в раздел **Logs**
3. Отфильтруйте по `/api/telegram/webhook`
4. Отправьте `/start` боту
5. Проверьте логи - должны появиться записи о получении обновления

**Ожидаемые логи:**
```
📥 Получено обновление от Telegram: { updateId: ..., hasMessage: true, messageText: '/start', ... }
📨 Processing /start command from user ...
📤 Sending welcome message to chat ...
✅ Welcome message sent successfully
```

## Если webhook не устанавливается

### Проверьте переменные окружения в Vercel

Убедитесь, что установлены:
- ✅ `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- ✅ `NEXT_PUBLIC_MINI_APP_URL` - URL мини-приложения (опционально)

### Проверьте токен бота

Убедитесь, что токен правильный:
```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe"
```

Должен вернуть информацию о боте.

### Проверьте URL webhook

Webhook URL должен быть:
- ✅ HTTPS (не HTTP)
- ✅ Публично доступен
- ✅ Правильный путь: `/api/telegram/webhook`

## Важные замечания

1. **Pending updates**: После установки webhook, Telegram отправит все накопленные обновления (16 шт. в вашем случае). Это нормально, они будут обработаны.

2. **Allowed updates**: Webhook настроен на обработку только сообщений (`["message"]`), что оптимально для вашего случая.

3. **Secret token**: Можно настроить дополнительную защиту через `TELEGRAM_SECRET_TOKEN` в переменных окружения.

## После успешной установки

- ✅ Webhook будет активен
- ✅ Бот начнет отвечать на команды
- ✅ Накопленные обновления будут обработаны
- ✅ Новые команды будут обрабатываться в реальном времени

