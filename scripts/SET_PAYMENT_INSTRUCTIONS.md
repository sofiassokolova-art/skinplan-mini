# Инструкция по установке статуса оплаты

## Способ 1: Через консоль браузера (самый простой)

1. Откройте Mini App в Telegram
2. Нажмите `F12` (или `Cmd+Option+I` на Mac) чтобы открыть консоль разработчика
3. Вставьте и выполните этот код:

```javascript
localStorage.setItem('payment_first_completed', 'true');
localStorage.setItem('payment_retaking_completed', 'true');
console.log('✅ Статус оплаты установлен!');
```

4. Обновите страницу (`F5` или `Cmd+R`)
5. План должен отобразиться без блюра

## Способ 2: Через HTML файл

1. Откройте файл `scripts/set-payment-status.html` в браузере
2. Нажмите кнопку "✅ Установить статус оплаты"
3. Вернитесь в Mini App и обновите страницу

**Важно:** HTML файл должен быть открыт в том же браузере, что и Mini App (или в том же окне).

## Способ 3: Через скрипт

Скопируйте содержимое файла `scripts/set-payment-console.js` и выполните в консоли браузера.

## Проверка статуса

Чтобы проверить, установлен ли статус оплаты, выполните в консоли:

```javascript
console.log('Первая оплата:', localStorage.getItem('payment_first_completed'));
console.log('Перепрохождение:', localStorage.getItem('payment_retaking_completed'));
```

## Очистка статуса

Чтобы очистить статус оплаты (для тестирования):

```javascript
localStorage.removeItem('payment_first_completed');
localStorage.removeItem('payment_retaking_completed');
console.log('🗑️ Статус оплаты очищен');
```
